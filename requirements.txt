# =============================================================
#  YouCaps — Python Dependencies
#  Installeer met: pip install -r requirements.txt
#
#  BELANGRIJK: gebruik een lockfile in productie!
#  Genereer met: pip freeze > requirements.lock
#  Of gebruik: uv pip compile requirements.txt -o requirements.lock
# =============================================================

# --- Fase 1: Bestaande dependencies (al in gebruik) ---
anthropic>=0.40.0,<1.0.0      # Claude AI SDK (sync + async client)
pydantic>=2.0.0,<3.0.0        # Data validatie & modellen
pydantic-settings>=2.0.0,<3.0.0  # Settings management (.env laden)
requests>=2.31.0,<3.0.0       # HTTP requests (Open Food Facts API, generator.py)

# --- Fase 2: Backend Foundation ---
fastapi>=0.111.0,<1.0.0       # Web framework
uvicorn[standard]>=0.29.0,<1.0.0  # ASGI server
sqlalchemy>=2.0.0,<3.0.0      # ORM
alembic>=1.13.0,<2.0.0        # Database migrations
python-dotenv>=1.0.0,<2.0.0   # .env bestandsbeheer
httpx>=0.27.0,<1.0.0          # Async HTTP client (ook voor tests)

# --- Fase 3: Security & Vision Scanner ---
python-jose[cryptography]>=3.3.0,<4.0.0   # JWT tokens
passlib[bcrypt]>=1.7.4,<2.0.0             # Wachtwoord hashing
slowapi>=0.1.9,<1.0.0                     # Rate limiting voor FastAPI
python-multipart>=0.0.9,<1.0.0            # multipart/form-data parsing (foto-upload)
Pillow>=10.3.0,<12.0.0                    # Image-validatie & preprocessing
rapidfuzz>=3.6.0,<4.0.0                   # Fuzzy matching voor ingredient-namen

# --- Fase 4: Oura-integratie ---
supabase>=2.0.0,<2.15.0           # Supabase Python client (wearable token storage; <2.15 avoids pyiceberg build dep)
# cryptography is already available via python-jose[cryptography] — used for AES-256-GCM

# --- Fase 5: Testing ---
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
resend>=2.0.0,<3.0.0
